n_outer_folds: 4
n_inner_folds: 4

n_trials: 20

param_sampler:
  _target_: optuna.samplers.TPESampler
  multivariate: true

sensor_aggregation_fn: mean

params:
  encoder__alpha:
    type: float
    low: 1e2
    high: 1e5
    log: true
    # shape: [129]

  # encoder__optim__learning_rate:
  #   type: float
  #   low: 1e-4
  #   high: 5e-1
  #   log: true
